import { FiltroEventosPipe } from './filtro-eventos.pipe';

describe('FiltroEventosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEventosPipe();
    expect(pipe).toBeTruthy();
  });
});
