import { TiempoEsperaDirective } from './tiempo-espera.directive';

describe('TiempoEsperaDirective', () => {
  it('should create an instance', () => {
    const directive = new TiempoEsperaDirective();
    expect(directive).toBeTruthy();
  });
});
